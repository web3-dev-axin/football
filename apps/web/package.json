{
  "name": "@worldcup/web",
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "test": "bun test test",
    "test:responsive": "bun scripts/responsive-smoke.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@worldcup/db": "workspace:*",
    "@worldcup/sdk": "workspace:*",
    "@worldcup/shared": "workspace:*",
    "next": "latest",
    "react": "latest",
    "react-dom": "latest",
    "viem": "^2.48.11"
  },
  "devDependencies": {
    "@types/node": "^25.9.0",
    "playwright": "^1.60.0"
  }
}
