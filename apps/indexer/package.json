{
  "name": "@polygoal/indexer",
  "type": "module",
  "scripts": {
    "dev": "ponder dev",
    "start": "ponder start",
    "serve": "ponder serve",
    "codegen": "ponder codegen",
    "smoke": "bun src/dev.ts",
    "test": "bun test src",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@polygoal/config": "workspace:*",
    "@polygoal/db": "workspace:*",
    "@polygoal/shared": "workspace:*",
    "ponder": "^0.16.6",
    "viem": "^2.48.11"
  }
}
