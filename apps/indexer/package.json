{
  "name": "@worldcup/indexer",
  "type": "module",
  "scripts": {
    "dev": "bun src/dev.ts",
    "reset": "bun src/reset.ts",
    "test": "bun test src",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@worldcup/config": "workspace:*",
    "@worldcup/db": "workspace:*",
    "@worldcup/shared": "workspace:*",
    "ponder": "^0.16.6",
    "viem": "^2.48.11"
  }
}
